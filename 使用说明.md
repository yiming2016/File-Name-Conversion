# 文件名翻译器

把文件夹里的文件名翻译成目标语言（默认英文/日文/韩文等 → 中文），支持批量预览、勾选、重命名和撤销。

## 功能

1. **翻译源**
   - 谷歌翻译（免费，无需 Key；国内网络一般需要代理，可在“管理翻译源 → 通用”里配置）
   - DeepL 官方 API（免费/专业版，需要 API Key）
   - Yandex 官方 API（需要 API Key，点击链接直达申请页）
   - 百度翻译 / 火山翻译 / 小牛翻译 / 腾讯云（国内翻译源，各自页签里填密钥即可）
   - AI 模型（任意 OpenAI 兼容接口：DeepSeek、OpenAI、通义千问、智谱、Kimi、本地 Ollama 等，只需填接口地址 + Key + 模型名）
   - 自定义翻译源（任意 HTTP 接口：URL/请求头/请求体/响应路径都可配置，内置 MyMemory 免费示例）
2. **语言设置**
   - 源语言：可**多选**（点“选择源语言...”），如同时勾选英文、日文、韩文；勾选“自动检测”则全部语言都翻
   - 多选指定语言时，翻译仍走自动检测，检测到不在所选范围的语言会标记为“源语言不在所选范围，跳过”（谷歌翻译返回真实检测结果，其他接口用文字体系辅助判断）
   - 目标语言：中文（默认）、英文、日文、韩文等几十种
   - 可选“跳过已经属于目标语言的文件”：例如目标是中文时，已是中文的文件名不会被翻译
3. **自定义词库 + 云端翻译**
   - 自定义词条（原文 → 译文），翻译时词条优先：先把原文替换成占位符，云端翻译完成后再还原成词库译文，保证专有名词不被乱翻
   - 三种匹配方式：子串匹配（默认）、整词匹配（按英文单词边界）、正则表达式（可用 `\1` 引用捕获组）
   - 译文与原文相同时表示“保留原名”（例如乐队名 Deep Purple → Deep Purple）
   - 界面内可直接增删改、调整顺序、导入/导出 CSV；也可以直接用 Excel 编辑 `词库\默认词库.csv`（UTF-8，表头：原文,译文,匹配方式,启用）
   - **词库文件目录** `词库/`：按格式分子文件夹 `tsv/ tbx/ tmx/ json/ csv/ txt/`，勾选即可启用，支持 TXT/CSV/TSV/JSON/TMX/TBX-Basic 六种格式（TMX/TBX 自动提取双语词对，单列词表按“保留原文”处理）
   - 词库文件以三级树形显示：**格式 > 文件夹 > 文件**（如 json > naughty-words-js > ar），鼠标滚轮和右侧滚动条均可查看
   - 默认内置两个 GitHub 词库项目（`管理词库 → 词库文件` 中可勾选/取消）：
     - LDNOOBW/List-of-Dirty-Naughty-Obscene-and-Otherwise-Bad-Words（txt，29 种语言）
     - LDNOOBW/naughty-words-js（json，28 种语言）
4. **图形界面**
   - 选择文件夹**或文件**（“选择文件...”可多选，多个路径用 `;` 分隔），包含子文件夹、按扩展名过滤（视频/音频/图片/文档预设或自定义）
   - 预览表格逐条显示翻译结果，点击行可勾选/取消
   - 批量重命名（自动处理重名冲突）、一键撤销上次重命名
   - 翻译记录自动写入文件夹下的 `重命名记录.log`
   - 设置窗口“测试翻译”成功后对应页签变绿、失败变红；AI 预设的获取 API Key 网址可直接点击用浏览器打开
   - AI 模型名称是**下拉框**：可直接输入自定义名称，或点“获取模型列表”自动读取该接口正在服务的模型；“余额”行可一键查询 API Key 余额（DeepSeek/通义千问等支持）
   - 彩色 UI：蓝色主题横幅 + OnlyFans 风格 Logo、**圆角按钮**、蓝色表格表头、彩色页签

## 运行

### 直接运行源码（需要 Python 3.8+）

```bat
cd /d C:\software\文件名翻译
python -m pip install -r requirements.txt
python main.py
```

### 打包成 exe

双击 `build_exe.bat`，完成后 exe 在 `dist\文件名翻译器.exe`，可复制到任意 Windows 电脑使用，无需安装 Python。

## 常见问题

- **谷歌翻译提示请求失败/超时**：国内直连谷歌通常不通。在“管理翻译源 → 通用”填入本地代理（如 `127.0.0.1:10808`，不带 http:// 会自动补全），或改用 DeepL / AI 接口。
- **DeepL 报“未配置 API Key”**：到 [DeepL API](https://www.deepl.com/zh/pro-api) 注册，把 Key 填进“管理翻译源 → DeepL”。
- **AI 接口怎么填**：选择预设（DeepSeek/通义千问等）会自动填好接口地址和模型名，只需填 API Key。本地 Ollama 可留空 Key。
- **自定义翻译源怎么配**：以 MyMemory 为例——
  - URL：`https://api.mymemory.translated.net/get?q={text}&langpair={source}|{target}`
  - 响应路径：`responseData.translatedText`
  - 支持 `{text}` `{source}` `{target}` 占位符；POST 请求可在“请求体”填 JSON 模板。
- **词库怎么用**：勾选主界面“启用自定义词库”，点“管理词库...”添加词条。例如想让 `NavEditor` 固定翻译成“导航编辑器”，添加原文 `NavEditor`、译文 `导航编辑器`。想让某个词完全保留，译文填与原文相同的词即可。需要精细控制时可切到“正则表达式”模式。
- **词库文件怎么用**：把词库文件按格式放进 `词库/` 下对应文件夹（如 `词库/json/`），在“管理词库 → 词库文件”勾选启用。TMX/TBX 文件会提取原文/译文词对；纯词表（每行一个词）按“保留原文”处理。内置的两个 GitHub 脏词库默认已勾选。
- **配置文件**：保存在 exe/脚本同目录的 `config.json`，删除后恢复默认设置。

## 目录结构

```
C:\software\文件名翻译\
├─ 成品-打开即用\          打开即用：文件名翻译器.exe + 词库\（含默认词库.csv）（整体复制到任意电脑即可）
│   └─ config.json        运行配置，首次打开自动生成
└─ 开发源码\              编程/打包用（平时不需要动）
    ├─ app\               源码（引擎/核心逻辑/界面/词库解析）
    ├─ main.py            程序入口（开发时可 python main.py 直接运行）
    ├─ build_exe.bat      重新打包 exe（自动输出到 成品-打开即用\）
    ├─ make_icon.py / requirements.txt / 文件名翻译器.spec   构建辅助
    ├─ 词库\               开发用词库副本（含默认词库.csv）
    ├─ 截图\               界面截图
    └─ 旧版脚本\           最初版本的 fast_rename.py 与说明
```

日常使用只进 `成品-打开即用\`；改代码或重新打包才需要 `开发源码\`。
